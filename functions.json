[
  {
    "name": "no_function_message",
    "description": "Reply to user message when the function calls below do not matches the users query.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_message": {
          "type": "string",
          "description": "Message from the user."
        }
      },
      "required": ["user_message"]
    }
  },
  {
    "name": "change_voice_style",
    "description": "Allows the user to change the tone and style of the current text to speech model based on a prompt.",
    "parameters": {
      "type": "object",
      "properties": {
        "voice_style": {
          "type": "string",
          "description": "Style description from the user. Example: A male voice that is calm but stern."
        }
      },
      "required": ["voice_style"]
    }
  },
  {
    "name": "describe_image",
    "description": "Answer user query about an image.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_query": {
          "type": "string",
          "description": "The query provided by the user."
        }
      },
      "required": ["user_query"]
    }
  },
  {
    "name": "create_image",
    "description": "Generate an image from a prompt.",
    "parameters": {
      "type": "object",
      "properties": {
        "image_prompt": {
          "type": "string",
          "description": "The prompt required to generate image."
        }
      },
      "required": ["image_prompt"]
    }
  },
  {
    "name": "video_player",
    "description": "Retrieve YouTube query and play the video in the chat.",
    "parameters": {
      "type": "object",
      "properties": {
        "youtube_query": {
          "type": "string",
          "description": "The YouTube query provided by the user."
        }
      },
      "required": ["youtube_query"]
    }
  }
]
